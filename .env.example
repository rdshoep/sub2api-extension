# Names only — never commit real values.
# Optional local mock / development overrides.

# Sub2API instance origin for manual read-only checks (no trailing secret).
SUB2API_BASE_URL=

# Admin API key name placeholder. Leave empty. Do not put a real key here.
SUB2API_ADMIN_API_KEY=

# Admin JWT name placeholder. Leave empty. Do not put a real token here.
SUB2API_JWT=

# Playwright / mock server
MOCK_SUB2API_PORT=4178
E2E_HARNESS_PORT=4177
