# PanelSpec reference

Validate against `schemas/panel-spec.schema.json`. Packed IDs only. See `docs/panel-spec.md`.

Required keys: `schemaVersion`, `pack`, `views`. Optional: `theme`, `queries`, `actions` with confirmation policy.
